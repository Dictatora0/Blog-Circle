# GaussDB 集群配置文件
# 使用方法：source .env.cluster 或 export $(cat .env.cluster | xargs)

# 主节点配置
PRIMARY_IP=10.211.55.11
PRIMARY_ROOT_PWD=747599qw@

# 备节点1配置
STANDBY1_IP=10.211.55.14
STANDBY1_ROOT_PWD=747599qw@1

# 备节点2配置
STANDBY2_IP=10.211.55.13
STANDBY2_ROOT_PWD=747599qw@2

# 数据库配置
GAUSSDB_PORT=5432
GAUSSDB_DATABASE=blog_db
GAUSSDB_USERNAME=bloguser
GAUSSDB_PASSWORD=blogpass

# 应用配置
BACKEND_PORT=8081
FRONTEND_PORT=8080
SPARK_MASTER_PORT=7077
SPARK_UI_PORT=8888

# Spring Profile
SPRING_PROFILES_ACTIVE=gaussdb-cluster
