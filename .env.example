# Blog Circle 环境变量配置模板
# 复制此文件为 .env.local 并填入实际值

# ============================================
# 虚拟机配置
# ============================================
VM_IP=10.211.55.11
VM_USER=root
VM_PASSWORD=your_vm_password_here
VM_PROJECT_DIR=/root/CloudCom

# ============================================
# Docker Compose 配置
# ============================================
COMPOSE_FILE=infra/docker-compose/opengauss-cluster-legacy.yml

# ============================================
# openGauss credentials & metadata
# ============================================
OPENGAUSS_PASSWORD=your_db_password_here
OPENGAUSS_DATABASE=blog_db
OPENGAUSS_USERNAME=bloguser
OPENGAUSS_PRIMARY_HOST=opengauss-primary
OPENGAUSS_STANDBY1_HOST=opengauss-standby1
OPENGAUSS_STANDBY2_HOST=opengauss-standby2

# ============================================
# openGauss ports (container + host)
# ============================================
OPENGAUSS_PRIMARY_PORT=5432
HOST_PRIMARY_DB_PORT=5432
OPENGAUSS_STANDBY1_PORT=15432
HOST_STANDBY1_DB_PORT=5434
OPENGAUSS_STANDBY2_PORT=25432
HOST_STANDBY2_DB_PORT=5436

# ============================================
# Analytics / Spark access
# ============================================
GAUSSDB_HOST=${VM_IP}
GAUSSDB_PRIMARY_PORT=5432
GAUSSDB_STANDBY1_PORT=5434
GAUSSDB_STANDBY2_PORT=5436
GAUSSDB_DATABASE=${OPENGAUSS_DATABASE}
GAUSSDB_USERNAME=${OPENGAUSS_USERNAME}
GAUSSDB_PASSWORD=${OPENGAUSS_PASSWORD}

# ============================================
# Backend/Frontend service ports
# ============================================
SPRING_PROFILES_ACTIVE=opengauss-cluster
BACKEND_CONTAINER_PORT=8080
HOST_BACKEND_PORT=8082
JAVA_TOOL_OPTIONS=-Xms64m -Xmx128m -XX:+UseSerialGC
FRONTEND_BACKEND_URL=http://backend:8080
FRONTEND_CONTAINER_PORT=8080
HOST_FRONTEND_PORT=8080

# ============================================
# 使用说明
# ============================================
# 1. 复制此文件: cp .env.example .env.local
# 2. 编辑 .env.local 填入实际密码
# 3. 在脚本中使用: source .env.local
# 4. .env.local 已在 .gitignore 中，不会被提交
